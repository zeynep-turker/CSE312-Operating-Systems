-I created i-nodes, data blocks, free blocks and i-nodes, directories and these structures are written into the entered file.
Since I wrote struct the file, the file is binary. Structs can be accessed with fread.
-After running the program with make, enter a command like makeFileSystem 4 400 mySystem.dat
